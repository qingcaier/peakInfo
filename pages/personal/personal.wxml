<view class="">
  <view>
    <!-- <button wx:if="{{!hasUserInfo && canIUse}}" open-type="getUserInfo" bindgetuserinfo="getUserInfo"> 获取头像昵称 </button>
    <block wx:else>
      <image bindtap="bindViewTap" class="userinfo-avatar" src="{{userInfo.avatarUrl}}" mode="cover"></image>
      <text class="userinfo-nickname">{{userInfo.nickName}}</text>
    </block> -->
    <!-- <block wx:if="{{!ifLogin}}"> -->
    <!-- <view class="noUser" wx:if="{{!hasUserInfo}}">
      <image class="wxlogo" src="{{wxlogo}}" />
      <button size="mini" type="primary" open-type="getUserInfo" bindgetuserinfo="getUserInfo">
        {{wxloginText}}
      </button>
    </view> -->
    <!-- <view class="userinfo" wx:else> -->
    <view class="userinfo">
      <image class="userinfo-avatar" src="{{userInfo.avatarUrl}}" />
      <view class="info-text">
        <text class="userinfo-nickname">{{userInfo.nickName}}</text>
        <view class="">
          <image wx:if="{{userInfo.gender}}" class="gender" src="{{boy}}" />
          <image wx:else class="gender" src="{{girl}}" />
        </view>
        <view class="joined-order">已拼{{userInfo.joined_order.length}}单</view>
      </view>
    </view>
  </view>
  <!-- <view class="black_line"></view> -->
  <view class="user_orders" bindtap="goMyOrder" data-index="0">
    <view class="">进行的拼单</view>
    <view class="">{{running_order.length}}</view>
  </view>
  <view class="user_orders" bindtap="goMyOrder" data-index="1">
    <view class="">完成的拼单</view>
    <view class="">{{end_order.length}}</view>
  </view>
  <view class="user_orders" bindtap="goMyOrder" data-index="2">
    <view class="">取消的拼单</view>
    <view class="">{{cansel_order.length}}</view>
  </view>
  <view class="user_orders" bindtap="goMyOrder" data-index="3">
    <view class="">过期的拼单</view>
    <view class="">{{over_ddl_order.length}}</view>
  </view>
  <!-- <view class="black_line"></view> -->
</view>